services:
  sitevik:
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      SITEVIK_ROOT: /site
      BIND_ADDR: 0.0.0.0:8080
      SITEVIK_SPA: "false"
    volumes:
      - ./test-site:/site:ro
